# اختبار الترجمة - Translation Test

## ✅ التحديثات التي تمت

### 1. توحيد نظام الترجمة
- ✅ استبدال `LanguageProvider` بـ `TranslationProvider` في `layout.tsx`
- ✅ الآن جميع المكونات تستخدم نفس نظام الترجمة
- ✅ ملف الترجمات الموحد: `src/i18n/translations.ts`

### 2. المكونات المحدثة
- ✅ `LanguageToggle` - زر تبديل اللغة
- ✅ `Orders Page` - صفحة الطلبات
- ✅ `Dashboard` - لوحة التحكم
- ✅ `Navbar` - شريط التنقل
- ✅ `Sidebar` - القائمة الجانبية

## 🧪 كيفية الاختبار

### الخطوة 1: تحديث الصفحة
```
Ctrl + F5 (أو Cmd + Shift + R على Mac)
```

### الخطوة 2: افتح Developer Tools
```
F12 أو Ctrl + Shift + I
```

### الخطوة 3: اذهب إلى Console
- ستجد تبويب "Console" في Developer Tools

### الخطوة 4: اضغط على زر الترجمة
- ابحث عن أيقونة 🌐 في الـ Header
- أو نص "العربية" / "English"

### الخطوة 5: تحقق من Console
يجب أن ترى:
```
Toggling language from en to ar
```

### الخطوة 6: تحقق من التغييرات
- النصوص يجب أن تتغير فوراً
- اتجاه الصفحة يجب أن يتغير (RTL/LTR)
- العنوان في التبويب يجب أن يتغير

## 🔍 التحقق من localStorage

في Console، اكتب:
```javascript
localStorage.getItem('language')
```

يجب أن يعرض: `"ar"` أو `"en"`

## 🔍 التحقق من document attributes

في Console، اكتب:
```javascript
console.log('Language:', document.documentElement.lang);
console.log('Direction:', document.documentElement.dir);
console.log('Has RTL class:', document.documentElement.classList.contains('rtl'));
```

### النتيجة المتوقعة للعربية:
```
Language: ar
Direction: rtl
Has RTL class: true
```

### النتيجة المتوقعة للإنجليزية:
```
Language: en
Direction: ltr
Has RTL class: false
```

## 📝 الترجمات المتاحة

### صفحة Orders (الطلبات)
| المفتاح | الإنجليزية | العربية |
|---------|------------|---------|
| `orders.title` | Orders | الطلبات |
| `orders.refresh` | Refresh | تحديث |
| `orders.table.id` | ID | الرقم |
| `orders.table.car` | Car | السيارة |
| `orders.table.from` | From | من |
| `orders.table.to` | To | إلى |
| `orders.table.price` | Price | السعر |
| `orders.table.status` | Status | الحالة |

### Dashboard (لوحة التحكم)
| المفتاح | الإنجليزية | العربية |
|---------|------------|---------|
| `dashboard.welcome` | Welcome to | مرحباً بك في |
| `dashboard.siteName` | Nukhbat Al-Naql | نخبة النقل |
| `dashboard.totalOrders` | Total Orders | إجمالي الطلبات |
| `dashboard.activeDrivers` | Active Drivers | السائقون النشطون |
| `dashboard.totalVehicles` | Total Vehicles | إجمالي المركبات |

### Navigation (التنقل)
| المفتاح | الإنجليزية | العربية |
|---------|------------|---------|
| `nav.dashboard` | Dashboard | لوحة التحكم |
| `nav.orders` | Orders | الطلبات |
| `nav.drivers` | Drivers | السائقين |
| `nav.vehicles` | Vehicles | المركبات |
| `nav.settings` | Settings | الإعدادات |

## 🐛 حل المشاكل

### المشكلة 1: الترجمة لا تتغير بعد الضغط على الزر

**الحل:**
1. امسح localStorage:
   ```javascript
   localStorage.clear()
   ```
2. حدّث الصفحة: `Ctrl + F5`
3. جرب مرة أخرى

### المشكلة 2: لا يوجد زر ترجمة

**الحل:**
- تحقق من أن `LanguageToggle` موجود في Navbar
- ابحث عن أيقونة 🌐
- قد يكون مخفي في قائمة الموبايل

### المشكلة 3: بعض النصوص لا تترجم

**الحل:**
- تحقق من أن المكون يستخدم `useTranslation()`
- تأكد أن المفتاح موجود في `translations.ts`
- تحقق من التهجئة الصحيحة

### المشكلة 4: RTL/LTR لا يعمل

**الحل:**
- تحقق من `document.documentElement.dir`
- تأكد أن CSS يدعم RTL
- استخدم Tailwind classes مثل `rtl:` و `ltr:`

## ✅ قائمة التحقق النهائية

- [ ] الصفحة تحدثت بنجاح (Ctrl + F5)
- [ ] زر الترجمة موجود ومرئي
- [ ] الضغط على الزر يظهر رسالة في Console
- [ ] النصوص تتغير من إنجليزي إلى عربي
- [ ] اتجاه الصفحة يتغير (RTL/LTR)
- [ ] localStorage يحفظ اللغة المختارة
- [ ] document.lang و document.dir محدثان
- [ ] العنوان في التبويب يتغير

## 📊 الإحصائيات

### عدد الترجمات المتاحة:
- **الإنجليزية**: ~200 مفتاح
- **العربية**: ~200 مفتاح

### الأقسام المترجمة:
- ✅ Common (عام)
- ✅ Navigation (التنقل)
- ✅ Sidebar (القائمة الجانبية)
- ✅ Auth (المصادقة)
- ✅ Dashboard (لوحة التحكم)
- ✅ Orders (الطلبات)
- ✅ Vehicles (المركبات)
- ✅ Drivers (السائقين)
- ✅ Clients (العملاء)

## 🎯 الخطوات التالية

إذا كانت الترجمة تعمل بشكل صحيح:
1. ✅ اختبر جميع الصفحات
2. ✅ تحقق من جميع المكونات
3. ✅ أضف ترجمات إضافية إذا لزم الأمر

## 📞 الدعم

إذا استمرت المشكلة:
1. أرسل screenshot من Console
2. أرسل نتيجة `localStorage.getItem('language')`
3. أرسل نتيجة `document.documentElement.lang`
